export const selectIsLoggedIn = (state) => state.streamers.isLoggedIn;

export const selectUser = (state) => state.streamers.user;

export const selectToken = (state) => state.streamers.token;

export const selectError = (state) => state.streamers.error;

export const selectIsLoading = (state) => state.streamers.isLoading;

export const selectStreamers = (state) => state.streamers.streamers;
